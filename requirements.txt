# ApplyPilot — Python dependencies
# Install with:  pip install -r requirements.txt
# Then:          pip install --no-deps python-jobspy && pip install pydantic tls-client requests markdownify regex
# (python-jobspy must be installed separately — see README)

# --- Core framework ---
typer>=0.9.0
rich>=13.0
python-dotenv>=1.0

# --- HTTP & scraping ---
httpx>=0.24
beautifulsoup4>=4.12
lxml>=4.9
requests>=2.31
markdownify>=0.11
playwright>=1.40

# --- Data ---
pyyaml>=6.0
pandas>=2.0
pydantic>=2.0

# --- API server ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Scheduler (cron every 6h) ---
apscheduler>=3.10.0

# --- Cloud / notifications ---
cloudinary>=1.40.0

# --- LLM providers (at least one required) ---
openai>=1.0.0
google-generativeai>=0.8.0
anthropic>=0.20.0
